---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: queueraws.sqs.aws.upbound.io
spec:
  group: sqs.aws.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: QueueRAW
    listKind: QueueRAWList
    plural: queueraws
    singular: queueraw
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: QueueRAW is the native (non-Terraform) Schema for AWS SQS Queues
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: QueueRAWSpec defines the desired state of QueueRAW.
            properties:
              forProvider:
                description: ForProvider holds the provider-specific configuration
                  for the resource.
                properties:
                  contentBasedDeduplication:
                    description: Enables content-based deduplication for FIFO queues.
                    type: boolean
                  deduplicationScope:
                    description: |-
                      Specifies whether message deduplication occurs at the message group or
                      queue level. Valid values are messageGroup and queue (default).
                    type: string
                  delaySeconds:
                    description: |-
                      Time in seconds that the delivery of all messages in the queue will be
                      delayed. An integer from 0 to 900 (15 minutes).
                    type: number
                  fifoQueue:
                    description: |-
                      Boolean designating a FIFO queue. If not set, it defaults to false making
                      it standard.
                    type: boolean
                  fifoThroughputLimit:
                    description: |-
                      Specifies whether the FIFO queue throughput quota applies to the entire
                      queue or per message group. Valid values are perQueue (default) and
                      perMessageGroupId.
                    type: string
                  kmsDataKeyReusePeriodSeconds:
                    description: |-
                      Length of time, in seconds, for which Amazon SQS can reuse a data key to
                      encrypt or decrypt messages before calling AWS KMS again.
                    type: number
                  kmsMasterKeyId:
                    description: |-
                      ID of an AWS-managed customer master key (CMK) for Amazon SQS or a
                      custom CMK.
                    type: string
                  maxMessageSize:
                    description: |-
                      Limit of how many bytes a message can contain before Amazon SQS rejects
                      it. An integer from 1024 bytes (1 KiB) up to 1048576 bytes (1024 KiB).
                    type: number
                  messageRetentionSeconds:
                    description: Number of seconds Amazon SQS retains a message.
                    type: number
                  name:
                    description: |-
                      Name of the queue. Queue names must be made up of only uppercase and
                      lowercase ASCII letters, numbers, underscores, and hyphens, and must be
                      between 1 and 80 characters long.
                    type: string
                  policy:
                    description: JSON policy for the SQS queue.
                    type: string
                  receiveWaitTimeSeconds:
                    description: |-
                      Time for which a ReceiveMessage call will wait for a message to arrive
                      (long polling) before returning. An integer from 0 to 20 (seconds).
                    type: number
                  redriveAllowPolicy:
                    description: JSON policy to set up the Dead Letter Queue redrive
                      permission.
                    type: string
                  redrivePolicy:
                    description: JSON policy to set up the Dead Letter Queue.
                    type: string
                  region:
                    description: Region where this resource will be managed. Required
                      for credential resolution.
                    type: string
                  sqsManagedSseEnabled:
                    description: |-
                      Boolean to enable server-side encryption (SSE) of message content with
                      SQS-owned encryption keys.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  visibilityTimeoutSeconds:
                    description: Visibility timeout for the queue. An integer from
                      0 to 43200 (12 hours).
                    type: number
                required:
                - region
                type: object
              initProvider:
                description: |-
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                properties:
                  contentBasedDeduplication:
                    description: Enables content-based deduplication for FIFO queues.
                    type: boolean
                  deduplicationScope:
                    description: Specifies whether message deduplication occurs at
                      the message group or queue level.
                    type: string
                  delaySeconds:
                    description: Time in seconds that the delivery of all messages
                      in the queue will be delayed.
                    type: number
                  fifoQueue:
                    description: Boolean designating a FIFO queue.
                    type: boolean
                  fifoThroughputLimit:
                    description: Specifies whether the FIFO queue throughput quota
                      applies to the entire queue or per message group.
                    type: string
                  kmsDataKeyReusePeriodSeconds:
                    description: Length of time, in seconds, for which Amazon SQS
                      can reuse a data key.
                    type: number
                  kmsMasterKeyId:
                    description: ID of an AWS-managed customer master key (CMK) for
                      Amazon SQS.
                    type: string
                  maxMessageSize:
                    description: Limit of how many bytes a message can contain before
                      Amazon SQS rejects it.
                    type: number
                  messageRetentionSeconds:
                    description: Number of seconds Amazon SQS retains a message.
                    type: number
                  name:
                    description: Name of the queue.
                    type: string
                  policy:
                    description: JSON policy for the SQS queue.
                    type: string
                  receiveWaitTimeSeconds:
                    description: Time for which a ReceiveMessage call will wait for
                      a message to arrive.
                    type: number
                  redriveAllowPolicy:
                    description: JSON policy to set up the Dead Letter Queue redrive
                      permission.
                    type: string
                  redrivePolicy:
                    description: JSON policy to set up the Dead Letter Queue.
                    type: string
                  sqsManagedSseEnabled:
                    description: Boolean to enable server-side encryption (SSE) of
                      message content with SQS-owned encryption keys.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  visibilityTimeoutSeconds:
                    description: Visibility timeout for the queue. An integer from
                      0 to 43200 (12 hours).
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: QueueRAWStatus defines the observed state of QueueRAW.
            properties:
              atProvider:
                description: AtProvider holds the provider-specific observation fields
                  for the resource.
                properties:
                  arn:
                    description: ARN of the SQS queue.
                    type: string
                  id:
                    description: URL for the created Amazon SQS queue (same as ID).
                    type: string
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: Map of tags assigned to the resource, including those
                      inherited from the provider.
                    type: object
                    x-kubernetes-map-type: granular
                  url:
                    description: URL for the created Amazon SQS queue.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
