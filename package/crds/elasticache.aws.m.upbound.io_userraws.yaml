---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: userraws.elasticache.aws.m.upbound.io
spec:
  group: elasticache.aws.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: UserRAW
    listKind: UserRAWList
    plural: userraws
    singular: userraw
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          UserRAW is the namespaced v1beta1 spoke for the native ElastiCache User resource.
          The hub version is UserRAW in namespaced v1beta2. Use v1beta2 for new resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserRAWSpec defines the desired state of namespaced UserRAW
              v1beta1 spoke.
            properties:
              forProvider:
                description: ForProvider holds the provider-specific configuration
                  for the resource.
                properties:
                  accessString:
                    description: Access permissions string used for this user.
                    type: string
                  authenticationMode:
                    description: |-
                      Denotes the user's authentication properties.
                      Note: v1beta1 uses a slice (array) while v1beta2 uses a pointer (object).
                    items:
                      description: |-
                        AuthenticationModeRAWParameters defines authentication mode parameters for namespaced UserRAW v1beta1.
                        Note: v1beta1 uses a slice (array) while v1beta2 uses a pointer (object).
                      properties:
                        passwordsSecretRef:
                          description: Specifies the passwords to use for authentication
                            if type is set to password.
                          items:
                            description: A SecretKeySelector is a reference to a secret
                              key in an arbitrary namespace.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          type: array
                        type:
                          description: 'Specifies the authentication type. Possible
                            options: password, no-password-required, iam.'
                          type: string
                      type: object
                    type: array
                  engine:
                    description: The current supported values are redis, valkey (case
                      insensitive).
                    type: string
                  noPasswordRequired:
                    description: Indicates a password is not required for this user.
                    type: boolean
                  passwordsSecretRef:
                    description: Passwords used for this user (top-level path, legacy).
                    items:
                      description: A SecretKeySelector is a reference to a secret
                        key in an arbitrary namespace.
                      properties:
                        key:
                          description: The key to select.
                          type: string
                        name:
                          description: Name of the secret.
                          type: string
                        namespace:
                          description: Namespace of the secret.
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    type: array
                  region:
                    description: Region where this resource will be managed.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  userName:
                    description: The username of the user.
                    type: string
                required:
                - region
                type: object
              initProvider:
                description: |-
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields.
                properties:
                  accessString:
                    type: string
                  authenticationMode:
                    items:
                      description: AuthenticationModeRAWInitParameters defines init
                        parameters for authentication mode (namespaced v1beta1 spoke).
                      properties:
                        type:
                          description: Specifies the authentication type.
                          type: string
                      type: object
                    type: array
                  engine:
                    type: string
                  noPasswordRequired:
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                  userName:
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: UserRAWStatus defines the observed state of namespaced UserRAW
              v1beta1 spoke.
            properties:
              atProvider:
                description: AtProvider holds the provider-specific observation fields
                  for the resource.
                properties:
                  accessString:
                    type: string
                  arn:
                    type: string
                  authenticationMode:
                    items:
                      description: AuthenticationModeRAWObservation defines observed
                        authentication mode state (namespaced v1beta1 spoke).
                      properties:
                        passwordCount:
                          type: number
                        type:
                          type: string
                      type: object
                    type: array
                  engine:
                    type: string
                  id:
                    type: string
                  noPasswordRequired:
                    type: boolean
                  status:
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                  userName:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: UserRAW is the native (non-Terraform) Schema for AWS ElastiCache
          User (namespaced, v1beta2 hub).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserRAWSpec defines the desired state of namespaced UserRAW
              (v1beta2 hub).
            properties:
              forProvider:
                description: ForProvider holds the provider-specific configuration
                  for the resource.
                properties:
                  accessString:
                    description: Access permissions string used for this user.
                    type: string
                  authenticationMode:
                    description: |-
                      Denotes the user's authentication properties.
                      Note: v1beta2 uses a pointer (object) while v1beta1 uses a slice (array).
                    properties:
                      passwordsSecretRef:
                        description: Specifies the passwords to use for authentication
                          if type is set to password.
                        items:
                          description: A SecretKeySelector is a reference to a secret
                            key in an arbitrary namespace.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        type: array
                      type:
                        description: 'Specifies the authentication type. Possible
                          options: password, no-password-required, iam.'
                        type: string
                    type: object
                  engine:
                    description: The current supported values are redis, valkey (case
                      insensitive).
                    type: string
                  noPasswordRequired:
                    description: Indicates a password is not required for this user.
                    type: boolean
                  passwordsSecretRef:
                    description: Passwords used for this user (top-level path, legacy).
                    items:
                      description: A SecretKeySelector is a reference to a secret
                        key in an arbitrary namespace.
                      properties:
                        key:
                          description: The key to select.
                          type: string
                        name:
                          description: Name of the secret.
                          type: string
                        namespace:
                          description: Namespace of the secret.
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    type: array
                  region:
                    description: Region where this resource will be managed.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  userName:
                    description: The username of the user.
                    type: string
                required:
                - region
                type: object
              initProvider:
                description: |-
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                properties:
                  accessString:
                    description: Access permissions string.
                    type: string
                  authenticationMode:
                    description: Denotes the user's authentication properties.
                    properties:
                      type:
                        description: 'Specifies the authentication type. Possible
                          options: password, no-password-required, iam.'
                        type: string
                    type: object
                  engine:
                    description: The current supported values are redis, valkey.
                    type: string
                  noPasswordRequired:
                    description: Indicates a password is not required.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  userName:
                    description: The username of the user.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: UserRAWStatus defines the observed state of namespaced UserRAW
              (v1beta2 hub).
            properties:
              atProvider:
                description: AtProvider holds the provider-specific observation fields
                  for the resource.
                properties:
                  accessString:
                    description: Access permissions string.
                    type: string
                  arn:
                    description: The ARN of the created ElastiCache User.
                    type: string
                  authenticationMode:
                    description: Denotes the user's authentication properties.
                    properties:
                      passwordCount:
                        description: Number of password values in use.
                        type: number
                      type:
                        description: The authentication type.
                        type: string
                    type: object
                  engine:
                    description: The cache engine.
                    type: string
                  id:
                    description: The user ID.
                    type: string
                  noPasswordRequired:
                    description: Indicates a password is not required.
                    type: boolean
                  status:
                    description: The current status.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags assigned to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  userName:
                    description: The username.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
