---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: statemachineraws.sfn.aws.m.upbound.io
spec:
  group: sfn.aws.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - aws
    kind: StateMachineRAW
    listKind: StateMachineRAWList
    plural: statemachineraws
    singular: statemachineraw
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: |-
          StateMachineRAW is the native (non-Terraform) Schema for AWS Step Functions
          State Machines API (namespaced scope).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              StateMachineRAWSpec defines the desired state of StateMachineRAW (namespaced scope).
              It reuses the cluster-native Parameters and InitParameters types so that both
              cluster and namespaced StateMachineRAW implement the shared StateMachineCR
              interface declared in the shared CRUD package.
            properties:
              forProvider:
                description: ForProvider holds the provider-specific configuration
                  for the resource.
                properties:
                  definition:
                    description: The Amazon States Language definition of the state
                      machine.
                    type: string
                  encryptionConfiguration:
                    description: Defines what encryption configuration is used to
                      encrypt data in the State Machine.
                    properties:
                      kmsDataKeyReusePeriodSeconds:
                        description: |-
                          Maximum duration for which Step Functions will reuse data keys. When the
                          period expires, Step Functions will call GenerateDataKey. This setting only
                          applies to customer managed KMS key and does not apply when type is
                          AWS_OWNED_KEY.
                        type: number
                      kmsKeyId:
                        description: |-
                          The alias, alias ARN, key ID, or key ARN of the symmetric encryption KMS key
                          that encrypts the data key.
                        type: string
                      kmsKeyIdRef:
                        description: Reference to a Key in kms to populate kmsKeyId.
                        properties:
                          name:
                            description: Name of the referenced object.
                            type: string
                          policy:
                            description: Policies for referencing.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        required:
                        - name
                        type: object
                      kmsKeyIdSelector:
                        description: Selector for a Key in kms to populate kmsKeyId.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                      type:
                        description: |-
                          The encryption option specified for the state machine.
                          Valid values: AWS_OWNED_KEY, CUSTOMER_MANAGED_KMS_KEY
                        type: string
                    type: object
                  loggingConfiguration:
                    description: Defines what execution history events are logged
                      and where they are logged.
                    properties:
                      includeExecutionData:
                        description: Determines whether execution data is included
                          in your log.
                        type: boolean
                      level:
                        description: |-
                          Defines which category of execution history events are logged.
                          Valid values: ALL, ERROR, FATAL, OFF
                        type: string
                      logDestination:
                        description: Amazon Resource Name (ARN) of a CloudWatch log
                          group.
                        type: string
                    type: object
                  publish:
                    description: Set to true to publish a version of the state machine
                      during creation.
                    type: boolean
                  region:
                    description: Region where this resource will be managed. Required
                      for credential resolution.
                    type: string
                  roleArn:
                    description: The Amazon Resource Name (ARN) of the IAM role to
                      use for this state machine.
                    type: string
                  roleArnRef:
                    description: Reference to a Role in iam to populate roleArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleArnSelector:
                    description: Selector for a Role in iam to populate roleArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  tracingConfiguration:
                    description: Selects whether AWS X-Ray tracing is enabled.
                    properties:
                      enabled:
                        description: When set to true, AWS X-Ray tracing is enabled.
                        type: boolean
                    type: object
                  type:
                    description: |-
                      Determines whether a Standard or Express state machine is created.
                      Valid values: STANDARD, EXPRESS.
                    type: string
                required:
                - region
                type: object
              initProvider:
                description: |-
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                properties:
                  definition:
                    description: The Amazon States Language definition of the state
                      machine.
                    type: string
                  encryptionConfiguration:
                    description: Defines what encryption configuration is used to
                      encrypt data in the State Machine.
                    properties:
                      kmsDataKeyReusePeriodSeconds:
                        description: Maximum duration for which Step Functions will
                          reuse data keys.
                        type: number
                      kmsKeyId:
                        description: The alias, alias ARN, key ID, or key ARN of the
                          symmetric encryption KMS key.
                        type: string
                      kmsKeyIdRef:
                        description: Reference to a Key in kms to populate kmsKeyId.
                        properties:
                          name:
                            description: Name of the referenced object.
                            type: string
                          policy:
                            description: Policies for referencing.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        required:
                        - name
                        type: object
                      kmsKeyIdSelector:
                        description: Selector for a Key in kms to populate kmsKeyId.
                        properties:
                          matchControllerRef:
                            description: |-
                              MatchControllerRef ensures an object with the same controller reference
                              as the selecting object is selected.
                            type: boolean
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: MatchLabels ensures an object with matching
                              labels is selected.
                            type: object
                          policy:
                            description: Policies for selection.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        type: object
                      type:
                        description: The encryption option specified for the state
                          machine.
                        type: string
                    type: object
                  loggingConfiguration:
                    description: Defines what execution history events are logged
                      and where they are logged.
                    properties:
                      includeExecutionData:
                        description: Determines whether execution data is included
                          in your log.
                        type: boolean
                      level:
                        description: Defines which category of execution history events
                          are logged.
                        type: string
                      logDestination:
                        description: Amazon Resource Name (ARN) of a CloudWatch log
                          group.
                        type: string
                    type: object
                  publish:
                    description: Set to true to publish a version of the state machine
                      during creation.
                    type: boolean
                  roleArn:
                    description: The Amazon Resource Name (ARN) of the IAM role to
                      use for this state machine.
                    type: string
                  roleArnRef:
                    description: Reference to a Role in iam to populate roleArn.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleArnSelector:
                    description: Selector for a Role in iam to populate roleArn.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  tracingConfiguration:
                    description: Selects whether AWS X-Ray tracing is enabled.
                    properties:
                      enabled:
                        description: When set to true, AWS X-Ray tracing is enabled.
                        type: boolean
                    type: object
                  type:
                    description: Determines whether a Standard or Express state machine
                      is created.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.definition is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.definition)
                || (has(self.initProvider) && has(self.initProvider.definition))'
          status:
            description: StateMachineRAWStatus defines the observed state of StateMachineRAW.
            properties:
              atProvider:
                description: AtProvider holds the provider-specific observation fields
                  for the resource.
                properties:
                  arn:
                    description: The ARN of the state machine.
                    type: string
                  creationDate:
                    description: The date the state machine was created.
                    type: string
                  definition:
                    description: The Amazon States Language definition of the state
                      machine.
                    type: string
                  description:
                    description: Description of the state machine version.
                    type: string
                  encryptionConfiguration:
                    description: Encryption configuration observed state.
                    properties:
                      kmsDataKeyReusePeriodSeconds:
                        description: Maximum duration for which Step Functions will
                          reuse data keys.
                        type: number
                      kmsKeyId:
                        description: The alias, alias ARN, key ID, or key ARN of the
                          symmetric encryption KMS key.
                        type: string
                      type:
                        description: The encryption option specified for the state
                          machine.
                        type: string
                    type: object
                  id:
                    description: The ARN of the state machine (also used as ID).
                    type: string
                  loggingConfiguration:
                    description: Logging configuration observed state.
                    properties:
                      includeExecutionData:
                        description: Determines whether execution data is included
                          in your log.
                        type: boolean
                      level:
                        description: Defines which category of execution history events
                          are logged.
                        type: string
                      logDestination:
                        description: Amazon Resource Name (ARN) of a CloudWatch log
                          group.
                        type: string
                    type: object
                  publish:
                    description: Whether a version was published on creation.
                    type: boolean
                  revisionId:
                    description: The ARN of the state machine version revision.
                    type: string
                  roleArn:
                    description: The Amazon Resource Name (ARN) of the IAM role.
                    type: string
                  stateMachineVersionArn:
                    description: The ARN of the state machine version.
                    type: string
                  status:
                    description: The current status of the state machine. Either ACTIVE
                      or DELETING.
                    type: string
                  tagsAll:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags including provider
                      defaults.
                    type: object
                    x-kubernetes-map-type: granular
                  tracingConfiguration:
                    description: Tracing configuration observed state.
                    properties:
                      enabled:
                        description: When set to true, AWS X-Ray tracing is enabled.
                        type: boolean
                    type: object
                  type:
                    description: The type of the state machine.
                    type: string
                  versionDescription:
                    description: Version description.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
